pass
## LimboHSM.gd
#extends LimboHSM  # LimboHSM should extend Node, or another appropriate base class
#
#@export var character : CharacterBody3D
#@export var states : Dictionary
#
#func _ready() -> void:
	#initialize(character)
	#set_active(true)
	#_binding_setup()
#
#func _binding_setup():
	## Make sure your states are added as nodes
	#add_transition(states["idle"], states["move"], "moving")
	#add_transition(states["move"], states["idle"], "stopped")
